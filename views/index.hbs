{{#> template}}

<h1>Puntajes pilotos</h1>
  <table class="table table-striped table-hover table-responsive">
    <thead>     
      <tr>
        <th>Pilotos/Circuitos</th>
        {{#each banderas}}
        <th><img src="{{this}}" alt=""  title=""srcset="" style="width: 30px; height: 15px;"></th>
        {{/each}}
      </tr>
    </thead>

    <tbody>
      {{#each pilotos}}
      <tr>
        <td>{{this}}
            {{#each ../pilotos}}
              <td>-</td>
              {{/each}}          
        </td>
      </tr>
      {{/each}}
    </tbody>
  </table>
  {{/template}}

